#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
# scale xps screen x2
monitor=desc:Samsung Display Corp. 0x4163,preferred,auto,2
# fwk screen
monitor=desc:BOE 0x0BCA,preferred,auto,1.33333
# home monitor
monitor=desc:LG Electronics LG HDR 4K 0x0003EE6B,preferred,auto-left,1.6
# alan's neighbouring desk
monitor=desc:LG Electronics LG ULTRAFINE 306MABT6SK41,preferred,auto-left,1.6
# carl's desk
monitor=desc:LG Electronics LG ULTRAFINE 306MAEG6SK32,preferred,auto-left,1.6
# asus monitor
monitor=desc:Ancor Communications Inc ASUS MG28U 0x0001052F,preferred,auto-left,1.6
# fallback for other screens (must be last to be configured)
monitor=,preferred,auto,1.666667


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,24

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = gb
    kb_variant =
    kb_model =
    kb_options = shift:both_capslock
    kb_rules =

    follow_mouse = 2

    touchpad {
        natural_scroll = no
        tap-and-drag = false
    }

    sensitivity = 0.25 # -1.0 - 1.0, 0 means no modification.
    accel_profile = adaptive
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 20
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10
    
    blur {
        enabled = true
        size = 3
        passes = 1
    }

    # drop_shadow = yes
    # shadow_range = 4
    # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = -1 # Set to 0 to disable the anime mascot wallpapers
    disable_splash_rendering = true
    disable_hyprland_logo = true
    mouse_move_focuses_monitor = false
}

xwayland {
  # disable scaling so that xwayland apps can use 4K
  force_zero_scaling = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
# device:epic-mouse-v1 {
#     sensitivity = -0.5
# }

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
binde =, XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.4 @DEFAULT_AUDIO_SINK@ 5%+
binde =, XF86AudioLowerVolume, exec, wpctl set-volume -l 1.4 @DEFAULT_AUDIO_SINK@ 5%-
bind =, XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

binde =, XF86MonBrightnessUp, exec, brillo -q -A 5
binde =, XF86MonBrightnessDown, exec, brillo -q -U 5

bind = CONTROL, SPACE, exec, swaync-client --close-latest

bind = $mainMod SHIFT, equal, exec, hyprlock

bind = $mainMod CONTROL, LEFT, movewindow, l
bind = $mainMod CONTROL, RIGHT, movewindow, r
bind = $mainMod CONTROL, UP, movewindow, u
bind = $mainMod CONTROL, DOWN, movewindow, d
binde = $mainMod, bracketright, resizeactive, 40 0
binde = $mainMod, bracketleft, resizeactive, -40 0
binde = $mainMod SHIFT, bracketright, resizeactive, 0 -40
binde = $mainMod SHIFT, bracketleft, resizeactive, 0 40
bind = $mainMod, F, fullscreen, 2
bind = $mainMod SHIFT, F, fullscreen, 1
bind = $mainMod, G, togglegroup
bind = $mainMod, Y, moveoutofgroup, active
bind = $mainMod SHIFT, H, movewindoworgroup, l
bind = $mainMod SHIFT, L, movewindoworgroup, r
bind = $mainMod SHIFT, J, movewindoworgroup, d
bind = $mainMod SHIFT, K, movewindoworgroup, u
bind = $mainMod CONTROL, H, swapwindow, l
bind = $mainMod CONTROL, J, swapwindow, d
bind = $mainMod CONTROL, K, swapwindow, u
bind = $mainMod CONTROL, L, swapwindow, r
bind = $mainMod CONTROL, semicolon, movegroupwindow, b
bind = $mainMod CONTROL, quoteright, movegroupwindow, f
bind = $mainMod, semicolon, changegroupactive, b
bind = $mainMod, quoteright, changegroupactive, f
bind = $mainMod, RETURN, exec, alacritty
bind = $mainMod, BACKSPACE, killactive, 
bind = , Print, exec, grim -g "$(slurp -d)" - | wl-copy
bind = $mainMod, M, exit, 
bind = $mainMod, V, togglefloating, 
bind = $mainMod, E, exec, rofi -show run -show-icons
bind = $mainMod SHIFT, E, exec, SUDO_ASKPASS=/home/james/bin/askpass-rofi rofi -dpi 1 -matching fuzzy -modi combi -show combi -combi-modi run,drun -run-command 'sudo -A {cmd}'
bind = $mainMod CONTROL, E, exec, rofi -show drun -show-icons
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, comma, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, focusworkspaceoncurrentmonitor, 1
bind = $mainMod, 2, focusworkspaceoncurrentmonitor, 2
bind = $mainMod, 3, focusworkspaceoncurrentmonitor, 3
bind = $mainMod, 4, focusworkspaceoncurrentmonitor, 4
bind = $mainMod, 5, focusworkspaceoncurrentmonitor, 5
bind = $mainMod, 6, focusworkspaceoncurrentmonitor, 6
bind = $mainMod, 7, focusworkspaceoncurrentmonitor, 7
bind = $mainMod, 8, focusworkspaceoncurrentmonitor, 8
bind = $mainMod, 9, focusworkspaceoncurrentmonitor, 9
bind = $mainMod, 0, focusworkspaceoncurrentmonitor, 10

# Move to next/previous workspace
bind=$mainMod SHIFT, comma, workspace, e-1
bind=$mainMod SHIFT, period, workspace, e+1

# Move active window to a workspace with mainMod + CONTROL + [0-9]
bind = $mainMod CONTROL, 1, movetoworkspacesilent, 1
bind = $mainMod CONTROL, 2, movetoworkspacesilent, 2
bind = $mainMod CONTROL, 3, movetoworkspacesilent, 3
bind = $mainMod CONTROL, 4, movetoworkspacesilent, 4
bind = $mainMod CONTROL, 5, movetoworkspacesilent, 5
bind = $mainMod CONTROL, 6, movetoworkspacesilent, 6
bind = $mainMod CONTROL, 7, movetoworkspacesilent, 7
bind = $mainMod CONTROL, 8, movetoworkspacesilent, 8
bind = $mainMod CONTROL, 9, movetoworkspacesilent, 9
bind = $mainMod CONTROL, 0, movetoworkspacesilent, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# bind power button
#bind = , XF86PowerOff, exec, touch /home/james/test

# Sends the active window to a "minimised" workspace and pops it off the stack back into current workspace
bind = $mainMod, W, movetoworkspacesilent, special:minimised
bind = $mainMod Control, W, togglespecialworkspace, minimised
bind = $mainMod Control, W, movetoworkspace, +0

bind = , XF86PowerOff, exec, wlogout

exec-once = dbus-update-activation-environment --systemd HYPRLAND_INSTANCE_SIGNATURE

exec-once = if [ $(hostname) != "lunar-fwk" ]; hyprlock; fi

exec-once = hyprshade auto

# This must go first for xdg-open events to use the lunar profile
exec-once = [workspace 3 silent] if [ $(hostname) == "lunar-fwk" ]; then firefox -P lunar; fi
exec-once = [workspace 4 silent] if [ $(hostname) == "lunar-fwk" ]; then slack; fi
exec-once = [workspace 1 silent] firefox -P personal
exec-once = [workspace 8 silent] discord --start-minimized
exec-once = waybar
exec-once = sleep 1; swaync
# (hack) because TS takes a while to initialise delay connecting to syncthing
# this should be solved via:
# > systemd.services.tailscaled.after = [ "systemd-networkd-wait-online.service" ];
# but this is broken on NixOS atm: https://github.com/NixOS/nixpkgs/issues/180175#issuecomment-2731401156
exec-once = sleep 5; syncthingtray --wait

# I think these services are starting before hyprland has finished
# initialising the IPC socket env var.
# They work once they've been restarted though
exec-once = sh -c "systemctl restart --user xremap.service nm-applet.service"

# fcitx5
exec-once=fcitx5 -d -r
exec-once=fcitx5-remote -r
